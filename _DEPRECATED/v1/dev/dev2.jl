## ------------------------------------------------------------------
@time begin
    using Plots
end

## ------------------------------------------------------------------
let
    bm(T, v) = (2*π*T)^(3/2) * v^2 * exp(-(v^2)/T)

    vs = range(0.0, 5.0; length = 100)
    bms = bm.(1, vs)
    
    plot(vs, bms)
end